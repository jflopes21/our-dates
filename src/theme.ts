import { extendTheme } from "@chakra-ui/react"

export const colors = {
  brand: {
    primary: '#EF5241',
    secondaryLight: '#544F4D',
    secondaryDark: '#EDF2F7',
  }
}

export const theme = extendTheme({colors})